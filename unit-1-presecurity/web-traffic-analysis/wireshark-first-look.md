# username=anna.virtanen&password=Summer2026!&remember=on
# POST
# Cookie: SESSIONID value  : a3f9c2e7b81d4f60a5e2c9d10f4b7e88.        an attacker can perform a session hijacking atttack
# Role: Finance Administrator ,  Email: anna.virtanen@pohjola-logistics.local ,  Last login from 10.10.10.50
# I cant find the username or the password because the application layer payload is encrypted
# https protocol, security layer: TLS , the file header specifies https_login in the windows title. the plaintext visible is the server hostname lab-portal.local exposed during the handshake
# the cookies are hidden from view. the dashboard data status completely protected.
# HTTp transmits data in clear text, allowing anyone intercepting the network traffic to read sensitive credentials and personal information, whereas HTTPS encrypts the data steam to ensure that only the authorized server can read it.
# public Wi-Fi network, what peotects me? HTTPS/TLS encryption secures passwords and session data. the website domain names remain visible in plaintext.
